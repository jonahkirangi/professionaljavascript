function addTen(num) {
  num += 10;
  return num;
}

var count = 20;
console.log(count);                   // The variable count is written to the console

var result = addTen(count);
console.log(result);                  // The function is called and the variable count is passed
