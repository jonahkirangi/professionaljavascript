// Create new array
var cities = ["Seattle", "Boston", "Phoenix", "Houston"];

// Use isArray mathod to confirm if cities is an array
if (Array.isArray(cities)) {
  // Print the third position of cities array
  console.log(cities[3]);
}

if (Array.isArray(cities)) {
  // Print entire cities array
  console.log(cities);
}
